<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- consume_list -->
<mapper namespace="com.bora.mapper.ConsumeMapper">
	<select id="getConsumeLastMonList" resultType="com.bora.domain.report.ConsumeLastThisListVO">
	   select bk_lastmon_num, bk_lastmon_category, bk_lastmon_total 
	   from consume_last_this_list
	   where id=#{loginID}
	   order by bk_lastmon_num desc
	</select>
	
	<select id="getConsumeThisMonList" resultType="com.bora.domain.report.ConsumeLastThisListVO">
	   select bk_thismon_num, bk_thismon_category, bk_thismon_total
	   from consume_last_this_list
	   where id=#{loginID}
	   order by bk_thismon_num desc
	</select>
	
	<select id="getConsumeAllList" resultType="com.bora.domain.report.ConsumeAllListVO">
	   select bk_consume_num, bk_category, bk_total_consume, bk_consume_compare 
	   from bora_consume_list
	   where id=#{loginID}
	   order by bk_consume_num desc
	</select>
	
	
</mapper>